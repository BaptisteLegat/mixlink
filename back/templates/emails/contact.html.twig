<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouveau message de contact</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        h1 {
            color: #6023c0;
            margin-bottom: 20px;
        }
        .sender-info {
            background-color: #f7f7f7;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 20px;
        }
        .message-content {
            background-color: #f0f0f0;
            padding: 15px;
            border-radius: 8px;
            white-space: pre-wrap;
        }
        .footer {
            margin-top: 30px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>
    <h1>Nouveau message de contact</h1>
    
    <div class="sender-info">
        <p><strong>Nom:</strong> {{ name }}</p>
        <p><strong>Email:</strong> {{ email }}</p>
        <p><strong>Sujet:</strong> {{ subject }}</p>
    </div>
    
    <h3>Message:</h3>
    <div class="message-content">
        {{ message|nl2br }}
    </div>
    
    <div class="footer">
        <p>Ce message a été envoyé via le formulaire de contact de Mixlink.</p>
    </div>
</body>
</html>
