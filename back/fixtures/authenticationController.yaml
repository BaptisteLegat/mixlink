include:
  - ./templates/provider.yaml
  - ./templates/user.yaml

App\Entity\Provider:
  spotify (extends provider_1):
    user: '@john-doe'

  youtube (extends provider_2):
    user: '@jane-smith'

  deezer (extends provider_3):
    user: '@alice-johnson'

App\Entity\User:
  john-doe (extends user_1): ~
  jane-smith (extends user_2): ~
  alice-johnson (extends user_3): ~
  bob (extends user_4): ~
